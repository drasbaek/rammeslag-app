# Runtime dependencies for the Vercel Python service.
# Mirrors [project].dependencies in pyproject.toml, which stays the source
# of truth for local development via uv. Parsed with tomllib, not a regex:
# a regex stops at the ] inside psycopg[binary] and silently drops the
# database driver.
fastapi>=0.115
pydantic>=2.9
pydantic-settings>=2.6
sqlalchemy>=2.0
alembic>=1.14
psycopg[binary]>=3.2
bcrypt>=4.2
itsdangerous>=2.2
